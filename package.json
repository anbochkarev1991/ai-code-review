{
  "name": "ai-code-review",
  "version": "1.0.0",
  "private": true,
  "description": "AI Code Review monorepo",
  "scripts": {
    "build:shared": "cd shared && npm install && npm run build",
    "build:backend": "cd backend && npm install && npx nest build",
    "build": "npm run build:shared && npm run build:backend",
    "start:backend": "cd backend && npm run start:prod"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
