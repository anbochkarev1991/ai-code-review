# Cursor IDE (keep .cursor/rules/ for versioned rules)
.cursor/*
!.cursor/rules/

# OS
.DS_Store

# Dependencies & build artifacts (safety net for all packages)
node_modules/
dist/

# Environment files (root-level)
.env
.env.local
